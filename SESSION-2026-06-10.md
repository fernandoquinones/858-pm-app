# Session log — June 10, 2026

## Shipped live (deployed on Vercel)
- **Anthropic key wired & working** — Generate with Claude + Add-to-plan with Claude.
- **Generate empty-plan bug fixed** — raised max_tokens to 16000 + guard against empty/truncated plans (was creating named projects with 0 tasks).
- **Activations bar fix** — extend + generate routes now write activation tags to project.activations so the top bar reflects what Claude added.
- **Delete events** from the homepage (master-only, cascade delete).
- **Reports & Views feature** — /api/report: Claude builds a self-contained HTML dashboard/calendar/timeline/report from live plan data; rendered in-app with open/download. Available to all roles, read-only.
- **Board redesign:**
  - Reports + Links moved into header pills that reveal an in-flow panel (~480px, right-aligned, pushes content down on open, pulls up on close, × to close).
  - "Add to plan with Claude" + "Add a task" side-by-side equal cards.
  - Add-task: full-width title + 3 equal labeled dropdowns (Owner, Workstream, Activation); activation collapses to "+ add/edit".
  - Filter bar: transparent, compact subtle pill chips.
  - Slack connect row: compact subtle chips, single line (shrink-to-fit).
- **Homepage:** "Project plans" header with **Current events / Past events** pills, auto-bucketed by event date (current = today/upcoming/+1-day grace/no-date; past = >1 day ago). Cards now show name, formatted date, city/state, editable venue, activation chips. Card refactored from Link wrapper to div (name + Open are links).
- **Event details:** Date + City + State + Venue.
  - Date, City, State **required** on both create paths (structured + Generate).
  - Venue is optional (TBD ok), editable after the fact on the homepage card AND the board header.
  - Editable Date/City/State/Venue in the board header.

## On hold / parked (built but NOT live)
- **Slack Phase 2 notifications** — DMs to owner, completion sync ("✓ Marked as complete" across channel + DM via button/reaction/app), Mark-complete button restored. INERT until: invite bot to a room + add im:write scope + populate slack_users. Two off-switches (no bot invite + no user IDs) keep it dormant.
- **comment-notify** (app comment -> Slack thread + @mention) — deleted file; spec captured as a task to rebuild later.
- **Activation-tasks bug** — selecting an activation in the header tags it but does NOT pull that activation's library tasks. Built a fix (/api/add-activation route + setEventActivations wiring) but **reverted/parked** — Fernando wants to think through the behavior. Route file app/api/add-activation/route.js exists on disk, unwired (don't push until revisited).

## Blocked / waiting
- **#40 date + phase logic** — waiting on Christina's refined Library-Tasks-Template.csv (task_type, timings, re-buckets, missing tasks). Then run 09-library-structure.sql, build importer, compute due dates from offsets + event date, auto pre/intra/post phase tag, recurring/spanning tasks.
- **Weekly digest engine (Mon/Wed/Fri)** — depends on #1 (DMs) + #40 (due dates). Mon "your week ahead", Wed reminder of incomplete, Fri reminder + next-week preview; stops on done or comment; to everyone, channel + DM.

## Queued (not started)
- Onsite logistics tracker view (data at task level, aggregated view).
- Cross-event / portfolio dashboard (extend Reports across all events).
- Workflow/dependency flowchart (after Christina's CSV).
- Logins/permissions (security). Client portal (Phase 6, Attio). Brief/overview + client meeting tracker -> fold into client portal.

## New files this session
- Reports route: app/api/report/route.js
- Slack: app/api/slack/complete/route.js; lib/slack.js dmUser + syncTaskCompleteMessages
- Parked: app/api/add-activation/route.js (unwired)
- Docs: HOW-TO-REVERT.md, Christina-Setup-Guide.docx (refreshed CHRISTINA-COEDIT-SETUP.md), Library-Tasks-Template.csv, Library-CSV-Column-Guide.md, supabase/09-library-structure.sql

## DB migrations run in Supabase this session
- 09-library-structure.sql (task_type, due/start offsets, recurrence on library_tasks + tasks)
- alter projects add column location text  (now superseded by venue/city/state — unused)
- alter projects add column venue/city/state text

## Gotchas / notes
- Old events created before date/location capture show "No date set" / blank city-state — not deleted; backfill SQL provided to copy old `location` -> city/state.
- Reveal panel: in-flow (pushes page), not overlay.
- Failsafe: Vercel Instant Rollback (Deployments -> previous -> Instant Rollback) or GitHub Desktop revert. See HOW-TO-REVERT.md.
